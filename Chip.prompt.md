Opal-trimmed pill with a colored leading dot, used to tag categories (the "what I make" list) or metadata.

```jsx
<Chip dotColor="var(--av-magenta)">gaming stream</Chip>
```

Always pairs a dot color from the core palette with a short label. Use several side by side with `gap: var(--space-3)` in a wrapping flex row.
