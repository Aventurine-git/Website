Small uppercase label that introduces a section — sits above a heading, never used alone.

```jsx
<Eyebrow color="var(--av-butter-deep)">still blooming</Eyebrow>
```

Always paired with a heading directly below. Pick a `-deep` color token so it stays readable on cream surfaces.
